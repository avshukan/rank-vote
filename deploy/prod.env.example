# Reference only. `make prod-config` generates independent random passwords
# directly in /etc/rank-vote/prod.env after merge. These placeholders fail validation.
DATABASE_URL=postgresql://rank_vote_app:GENERATE@postgres:5432/rank_vote_prod?schema=public
PORT=3000
CORS_ORIGIN=https://rank-vote.avshukan.com
TRUSTED_PROXY_HOPS=1
POSTGRES_BOOTSTRAP_PASSWORD=GENERATE
POSTGRES_APP_PASSWORD=GENERATE
