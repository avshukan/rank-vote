// Validation constants shared across the API and web app.
// See docs/01-mvp-scope.md and AGENTS.md "Key Constraints".

/** Minimum number of options a poll may have. */
export const MIN_OPTIONS = 2;

/** Maximum number of options a poll may have. */
export const MAX_OPTIONS = 10;
